# requirements.txt
# این فایل لیست تمام کتابخانه‌های بیرونی (Third-party libraries) است که پروژه نیاز دارد.
# با دستور "pip install -r requirements.txt" همه‌ی این‌ها یکجا نصب می‌شوند.

# --- کتابخانه‌ی اصلی ربات تلگرام ---
# برای ساخت ربات، مدیریت پیام‌ها، و دکمه‌های Inline Keyboard
python-telegram-bot==21.6

# --- گرفتن داده از فید JSON خبری ---
# برای ارسال درخواست HTTP و گرفتن داده از ForexFactory
requests==2.32.3

# --- زمان‌بندی کارها ---
# برای اجرای خودکار "چک کردن خبر جدید" هر N دقیقه
APScheduler==3.10.4

# --- خواندن متغیرهای محیطی از فایل .env ---
# برای نگه‌داری امن توکن‌ها و تنظیمات حساس، بدون قرار دادن مستقیم در کد
python-dotenv==1.0.1

# --- مدیریت Timezone ---
# چون اخبار اقتصادی با timezone های مختلف می‌آیند (مثلاً -04:00)
# و باید آن‌ها را به‌درستی به وقت محلی تبدیل کنیم
pytz==2024.2